@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    <div class="row">
        <div class="col">
            <h1>Register</h1>
            @await Html.PartialAsync("_Register")
        </div>
        <div class="col">
            <h1>Log in</h1>
            @await Html.PartialAsync("_Login")
        </div>
    </div>
</div>
